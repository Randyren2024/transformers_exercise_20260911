import numpy as np

# Token IDs from the tokenizer
token_ids = [0, 1, 2]

# Embedding matrix
embedding_matrix = np.array([
    [0.2, 0.1, 0.7, 0.3],
    [0.8, 0.2, 0.4, 0.1],
    [0.3, 0.9, 0.1, 0.6]
])

print("Embedding Matrix:")
print(embedding_matrix)